# 07-22-26 — The monolith dies with its contracts intact

One session, one arc: the 2,526-line `browser.rs` — flagged as the weakest code
in the repo — was dismantled into eight modules behind a wall of tests, with
zero UI change and zero contract change. The operator's brief was "code safety
is everything"; the session answered with a researched roadmap doc, then
executed Phases 0–4 of it back-to-back. Along the way it found two latent ACL
bugs, discovered CI had been silently red for four days, and fixed a real
cross-platform process-leak bug. Everything went straight to main; the tree
ends green everywhere that gates.

## 1. The roadmap (`b58e2d4`, `BROWSER_SHELL_REBUILD_ROADMAP.md`)
Three explorers mapped the monolith function-by-function, every JS/Elixir
caller contract, and the test/CI landscape; a plan agent turned that into the
phased design. Load-bearing decisions: the 33 commands never leave
`browser/mod.rs` (so `generate_handler!`/`build.rs`/capabilities stay closed
the whole time — a July-17 recurrence made structurally impossible); no cargo
workspace, no mock-webview trait — functional core / imperative shell instead;
tests written against the monolith *before* any code moves.

## 2. Phase 0 — characterization freeze (`3c96b7e`, `74e6049`, `8b67398`)
Planning itself surfaced a live bug: **`speak`/`stop_speaking` were missing
from `build.rs`** and only worked via a stale autogenerated permission toml —
the exact 07-17 failure class, still happening. Fixed in its own commit. Then
the freeze: `tests/acl_lockstep.rs` parses `generate_handler!`, `build.rs`,
and both capability files and asserts three-way lockstep (negative-tested:
deleting an allow entry fails the suite); 13 characterization tests pinned
`BrowserState` bookkeeping, geometry, URL gating, serde field names, and the
wait contract. The crate now passes `cargo fmt --check` and
`clippy -D warnings` (the churn was one comment and ~17 mechanical fixes).

## 3. Phase 1 — the gate (`5853f65`)
`ci.yml` gained a `rust` job (macos-14 — the objc path is the shipped path)
and a `js` job (the bun tests stop being decorative). `scripts/check_rust.sh`
is the single artifact CI, `mix precommit`, and QUALITY.md all describe.
Toolchain pinned via `rust-toolchain.toml` (1.97.1).

## 4. The four-day red CI nobody saw (`8324dd1`, `bd425b2`)
Watching the first gated run exposed that **main's CI had been failing since
~07-18**: fresh `mint` advisories (one HIGH) blocked the audit step on every
push. Fixing that (mint 1.9.3) let `mix test` reach a UTC/Linux runner for the
first time in days, which unmasked three failure classes:
- **A real bug**: the agent-runner group kill used `/bin/kill -KILL -<pgid>` —
  BSD syntax. procps kill on Linux fails *silently* on the bare negative pgid,
  so timed-out agent runs leaked their grandchildren on Linux. POSIX `--`
  fixes it; the existing reap test now proves it on ubuntu.
- Three calendar tests hard-coded `-07:00` fixtures (Pacific-only). New
  `BusterClaw.LocalTimeFixtures.local_rfc3339/1` renders fixtures in the
  runtime's own offset; verified under local, UTC, and Tokyo.
- The home-shell test assumed `claude` on PATH. `AgentRunner.detect/0` gained
  a documented `:agent_cli` app-env seam.
Result: main fully green (Build & verify + Rust + JS) for the first time since
07-18.

## 5. Phases 2–4 — the split (`fa7135a`, `866dba8`, `b7a741d`, `9d5d8cd`)
Move-only commits, content-anchored extraction, `--color-moved` reviewed, and
the Phase-0 tests passed unmodified through every move — the freeze doing its
job. Final shape: `mod.rs` 934 (thin commands + structs + menu routing),
`js.rs` 582 (injected scripts + escaping), `state.rs` 477 (bookkeeping + the
new pure `select_evictions` — eviction policy now has 5 tests of its own),
`webviews.rs` 364 (lifecycle loops, deliberately too dumb to be wrong),
`ffi.rs` 285 (the four objc operations, threading contract documented),
`notify.rs` 172, `labels.rs` 82, `geometry.rs` 67. The pure modules carry a
`pure_modules_have_no_tauri_imports` guard, so the architecture is enforced,
not aspirational. 31 Rust tests, up from 8.

## Where it stands
- Phases 0–4 shipped; the browser shell has real tests, a real CI gate, and
  no monolith.
- **Owed by operator**: the manual `./scripts/dev.sh` browser checklist
  (7-tab eviction churn, click/fill/wait/extract, screenshot, download +
  reveal, sidebar, zoom, find count, popup-as-tab, menu shortcuts) — the
  human pass over the live app before Phase 5.
- **Phase 5 remains**: terminal pure-helper tests + `smoke_desktop.sh`
  against a packaged build (closes the LEFTOVERS real-desktop smoke item).
- Noted for later: `block v0.1.6` is flagged future-incompatible — one more
  reason to modernize the objc corner eventually; Dialyzer stays known-red
  and non-blocking.
